import Wrapper from "../Wrappers/Wrapper"

export default function Beta(){
    return (
        <Wrapper>
            <div className="text-center py-48 text-center py-8 flex flex-col gap-2">
                <h3>
                    Hey, were in beta right now, so no sign up, but we apreciate you checking it out (we'll have it done in the next week or two)
                </h3>
            </div>
        </Wrapper>
    )
}